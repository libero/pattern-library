{%- set attributes = attributes|default({})|merge({datetime: date|date("Y-m-d")}) -%}

{% spaceless %}

  <time {%- include 'atoms-attributes' %}>

    <bdo>{{ date|localizeddate(format|default('long'), 'none', attributes.lang|default(lang ?? null)) }}</bdo>

  </time>

{% endspaceless %}
